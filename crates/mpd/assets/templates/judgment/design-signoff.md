# Design sign-off

## Actor

<!-- Exact cooperative actor label passed to `mpd gate`; not authenticated. -->

## Implementation vs intent

<!-- The built implementation checked against the mock and the intent: it is what
was designed, every feature is properly and elegantly represented, and nothing
regressed the existing design language. -->

## Verdict

<!-- PASS / CONDITIONAL PASS / FAIL. Sign-off gates Test — no sign-off, no Test.
A FAIL returns the implementation to the Builder. -->
