# Test report

## Coverage

<!-- What was tested: functional and non-functional (performance, load/stress,
resource, accessibility) plus fuzz/property/metamorphic passes for any
parser/interpreter/serializer/codec/protocol. Name the regression tests added. -->

## Results

<!-- The full-suite outcome with a real, non-zero count (pass/fail/skip), the
command shown, and any bug found + the regression test that pins it. -->

## Verdict

<!-- PASS / CONDITIONAL PASS / FAIL. All tests pass before deployment; a FAIL
returns to the earliest affected phase. -->
