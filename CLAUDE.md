# Commando — Claude Code Instructions

Before making any change in this repository, read `AGENTS.md` and follow its
required operating loop (`mpd conduct` → `mpd next` → work → `mpd gate` until
Done, then archive/commit/push/`mpd publish --verify`).

Claude Code sessions pass `--harness claude-code` wherever a harness is
required; see "Harness and model selection" in `AGENTS.md` for the per-harness
model mapping.
