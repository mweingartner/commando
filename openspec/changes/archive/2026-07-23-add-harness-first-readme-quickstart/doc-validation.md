# Doc validation: Harness-first README quickstart

## Actor

DocValidator-Sol-74

## Architect lens

Verified the README and generated change documentation against current `conduct` and
`next --harness` help, AGENTS harness rules, the existing trust boundary, ordered phases,
deployment authority, and manual `Everyday flow`. Commands and harness ownership are exact;
no runtime capability or release permission is implied. The section is one bounded hunk in
the declared README path, and all referenced headings exist.

## Designer lens

The new section appears immediately after MPD's value statement. Its first line gives the
user/model responsibility split, the two numbered steps require one placeholder edit, and
the two prompt blocks have unambiguous harness labels. Failure/setup and external-release
states remain explicit in text. The surface is semantic, screen-reader friendly, narrow-
layout tolerant, and leaves advanced detail below the approachable path.

Focused checks passed 15 content/structure/command assertions, doc staleness passed 19
Markdown files, and whitespace validation passed.

## Verdict

PASS
